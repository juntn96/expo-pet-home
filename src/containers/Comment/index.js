import React, { Component } from "react";
import { View, Text } from "react-native";
import Comment from "../../components/CommentScreen";

class Comment extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return <Comment />;
  }
}

export default Comment;
